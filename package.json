{
  "name": "chronium-mind",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Historical web and public-record intelligence aggregator",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/index.js && node --check src/schema.js && node --check public/app.js && node --check public/db.js && node --check public/ingest/adapters.js && node --check public/ingest/pipeline.js"
  },
  "devDependencies": {
    "fflate": "^0.8.3",
    "mammoth": "^1.12.1",
    "pdfjs-dist": "^6.2.108",
    "wrangler": "^4.31.0",
    "xlsx": "^0.18.5"
  }
}
